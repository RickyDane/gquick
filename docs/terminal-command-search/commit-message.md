feat(search): add terminal command mode

Allow main search to run terminal commands with the `>` prefix, either
in an external terminal or inline with captured output. Translation now
uses `t:` and `tr:` so command entry has a dedicated shortcut.
